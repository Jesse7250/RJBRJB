# EduHive 夜间自主迭代进度报告

**时间**：2026-06-23 02:13:01  
**迭代目标**：资源生成结果持久化缓存、降低重复 LLM 调用、全量测试验证

## 本次完成内容

### 1. 持久化资源缓存
- 新增 `backend/app/services/resource_cache.py`：
  - 使用现有 SQLite 数据库，新增 `resource_cache` 表。
  - key = 知识点 + 画像关键字段哈希。
  - 仅缓存辩论状态为 `PASSED`/`MODIFIED` 的资源。
  - 提供 `get_cached_resource`、`set_cached_resource`、`clear_cache`、`get_cache_stats`。
- 在 `AgentOrchestrator` 中：
  - 将原来的内存 dict 缓存替换为持久化缓存服务。
  - 同步与异步资源生成流程均读取/写入持久化缓存。
- 效果：
  - 同一知识点二次生成几乎 0 延迟（缓存命中）。
  - 进程重启后缓存仍然有效。
  - `test_ablation.py` deepseek 分支从 30-70s 降至 **0.02s**（缓存命中）。

### 2. 缓存阶段事件
- SSE 流式接口 `/stream-generate` 命中缓存时发送 `cache` 阶段事件：
  - `{"type":"progress","stage":"cache","message":"命中资源缓存，直接使用已审核通过的资源。"}`

## 测试结果

| 测试脚本 | 结果 | 关键观察 |
|---|---|---|
| `test_full_chain.py` | ✅ 通过 | 0.02s（缓存命中） |
| `test_chat_stream.py` | ✅ 通过 | SSE 对话正常 |
| `test_sse.py` | ✅ 通过 | 命中缓存，立即返回 |
| `test_code_executor.py` | ✅ 通过 | 执行/判题正常 |
| `test_evaluator.py` | ✅ 通过 | 评估链路正常 |
| `test_cache.py` | ✅ 通过 | 持久化缓存命中 |
| `test_safety_filter.py` | ✅ 通过 | 安全过滤正常 |
| `test_ablation.py` | ✅ 通过 | mock 0.03s，deepseek 0.02s（均缓存命中） |
| `npm run build` | ✅ 通过 | 无 chunk 警告 |

## 当前活跃问题

1. **缓存无 TTL**：旧缓存长期有效，若 LLM/Prompt 升级可能导致 stale 结果。后续可加入 `created_at` 过期策略。
2. **API key 明文存储**：`.env` 中仍为明文，已确认 `.gitignore` 包含 `.env`。
3. **Windows 终端中文乱码**：部分 PowerShell 输出显示乱码，属编码问题，不影响实际运行。

## 下一步计划（可选）

- 为资源缓存增加 TTL 过期策略。
- 接入真实 Neo4j（需 Docker 环境）。
- 增加 JWT 认证与用户体系。
- 根据认知风格动态调整前端布局。

## 代码变更摘要

- `backend/app/services/resource_cache.py`：新增持久化资源缓存服务。
- `backend/app/agents/orchestrator.py`：资源缓存从内存 dict 切换为 SQLite 持久化缓存。
