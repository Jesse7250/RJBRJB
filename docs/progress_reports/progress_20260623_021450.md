# EduMate 夜间自主迭代进度报告

**时间**：2026-06-23 02:14:50  
**迭代目标**：资源缓存 TTL 过期策略、防止 stale 缓存

## 本次完成内容

### 1. 缓存 TTL 过期策略
- `backend/app/services/resource_cache.py` 的 `get_cached_resource` 增加 `max_age_hours` 参数（默认 7 天）。
- 读取缓存时：
  - 解析 `created_at`。
  - 若超过 TTL，自动从 `resource_cache` 表中删除该记录并返回 `None`。
- 这样可以在保留缓存加速效果的同时，避免 Prompt/LLM 升级后返回过期资源。

### 2. TTL 测试
- 扩展 `backend/test_cache.py`：
  - 新增 `test_resource_cache_ttl()`，手动将缓存 `created_at` 改为 8 天前，验证过期后返回 `None`。

## 测试结果

| 测试脚本 | 结果 | 关键观察 |
|---|---|---|
| `test_full_chain.py` | ✅ 通过 | 0.03s（缓存命中） |
| `test_chat_stream.py` | ✅ 通过 | SSE 对话正常 |
| `test_sse.py` | ✅ 通过 | 缓存命中 |
| `test_code_executor.py` | ✅ 通过 | 执行/判题正常 |
| `test_evaluator.py` | ✅ 通过 | 评估链路正常 |
| `test_cache.py` | ✅ 通过 | 含 TTL 过期测试 |
| `test_safety_filter.py` | ✅ 通过 | 安全过滤正常 |
| `test_ablation.py` | ✅ 通过 | mock/deepseek 均 0.02s（缓存命中） |
| `npm run build` | ✅ 通过 | 无 chunk 警告 |

## 当前活跃问题

1. **API key 明文存储**：`.env` 中仍为明文，已确认 `.gitignore` 包含 `.env`。
2. **Windows 终端中文乱码**：部分 PowerShell 输出显示乱码，属编码问题，不影响实际运行。

## 下一步计划（可选）

- 接入真实 Neo4j（需 Docker 环境）。
- 增加 JWT 认证与用户体系。
- 根据认知风格动态调整前端布局。
- 将缓存 TTL 做成可配置项（`backend/.env`）。

## 代码变更摘要

- `backend/app/services/resource_cache.py`：新增 TTL 检查与过期删除。
- `backend/test_cache.py`：新增 TTL 过期测试。
